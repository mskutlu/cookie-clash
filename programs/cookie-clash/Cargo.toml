[package]
name = "cookie-clash"
version = "0.1.0"
description = "Cookie Clash — wagered on-chain tic-tac-toe for Cookie Chain"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "cookie_clash"

[dependencies]
anchor-lang = "1.2.0"

[features]
idl-build = ["anchor-lang/idl-build"]
